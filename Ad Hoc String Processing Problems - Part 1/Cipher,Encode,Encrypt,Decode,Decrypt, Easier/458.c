#include <stdio.h>
int main()
{
    char c;
    while(scanf("%c", &c)==1 )
    {
        if(c == '\n')
            printf("\n");
        else
            printf("%c",ch-7);
    }
    return 0 ;
}
